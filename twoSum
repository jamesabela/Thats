def twoSum(array1, target):
    for i in range(0, len(array1) - 1):
        for j in range(1, len(array1)):
            if array1[i] + array1[j] == target:
                returnstring = str(array1[i]) + ' and ' + str(array1[j])
                return returnstring
    return 'No solution found'
